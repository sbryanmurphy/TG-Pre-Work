let tool = '';

tool = 'marker';

// Use short circuit evaluation to assign writingUtensil variable below:
let writingUtensil = tool || 'marker';

console.log(`The ${writingUtensil} is mightier than the sword.`);

//started learning about truthy and falsy statements. To be honest, I'm having trouble grasping the difference between truthy/falsy and true/false...
