const fruits = ['mango', 'papaya', 'pineapple', 'apple'];

// Iterate over fruits below
fruits.forEach(fruit => console.log(`I want to eat a ${fruit}.`))

//learned about the foreach method, which is a simple way to loop through an array and execute the callback function for individual elements
