const logVisibleLightWaves = () => {
  const lightWaves = 'Moonlight';
  console.log(lightWaves);
};

logVisibleLightWaves();

// console.log(lightWaves);

//learned about block scope, which is sorta the opposite of global scope. block scope is only accessible to the code inside the block
